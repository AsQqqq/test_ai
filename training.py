from time import sleep

def generate_data() -> None:
    collection = []
    for i, j in zip(range(1, 201), range(1, 201)):
        list_numbers = []
        list_numbers.append(i)
        list_numbers.append(j)
        collection.append(list_numbers)
        

def hub_generator() -> None:
    pass
